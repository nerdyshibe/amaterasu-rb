#!/usr/bin/env ruby

require_relative '../lib/akane'
require 'stackprof'

options = Akane::CLI.parse(ARGV)

StackProf.run(mode: :object, out: 'tmp/stackprof-object.dump') do
  Akane::Emulator.start(**options)
end
